#include <stdio.h>
#include <stdlib.h>

int main(void){

   int i, j; 
   float x, y;

   //arbitrary integer values
   i = 10;
   j = 20;

   //arbitrary float values
   x = 43.2892f;
   y = 5527.0f;

   //function prints variable names and their corresponding values
   printf("i = %d, j = %d, x = %f, y = %f\n", i, j, x, y);
}
